{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npx esbuild api/index.ts --bundle --platform=node --format=cjs --outfile=api/index.js --packages=external",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
