{
  "version": 2,
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["**/*.ts", "package.json"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api" }
  ]
}
